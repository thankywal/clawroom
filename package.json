{
  "name": "clawroom",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check && vite build",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.worker.json",
    "deploy": "npm run build && wrangler deploy",
    "verify": "node scripts/verify.mjs",
    "ablate": "node scripts/ablate.mjs https://clawroom.thankywal-bkk.workers.dev/ablate.html?auto=1&n=5",
    "foreign": "node scripts/foreign-agent.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "wrangler": "^4.127.0"
  }
}